# BMAD Spec & Quality Standards

- **No Spec, No Code**: Never accept or trigger implementation without a validated specification or benchmark target.
- **Contract-First**: Public interfaces, data models, and API endpoints must be fully specified before internal logic is written.
- **Measurable Acceptance Criteria**: Every task plan generated by `@planner` must include verifiable verification steps for `@reviewer`.